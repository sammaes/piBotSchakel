// Clonen van git naar computer
git clone https://[sammaes92]@code.google.com/p/canbot-2014-2015-team-d

// File/Directory aanmaken zoals anders

// Files toevoegen aan lokale git
git add [files die je aangemaakt/aangepast hebt]

// Commit files in lokale git
git commit -m "beschrijving van files die toegevoegd/gewijzigd worden"

// Push files naar centrale git
git push

// Mappen op eigen computer updaten
In terminal in map "canbot-2014-2015-team-d" gaan en dan volgende commando gebruiken:
git pull
